version: '3.8'

services:

  go-cart-api:
    container_name: go-cart-api
    image: christianprzybulinski/builder
    build: 
      context: .
      dockerfile: docker/go-cart-api
    env_file: .env
    networks:
      - default
    ports:
    - "${API_PORT}:${API_PORT}"
    restart: on-failure
    volumes:
      - .:${APP_PATH}/go-cart-api