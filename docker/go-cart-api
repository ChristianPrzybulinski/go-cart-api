FROM golang:1.17-alpine as builder

RUN apk update && apk add --no-cache git

WORKDIR /go-cart-api

COPY go.mod ./
COPY go.sum ./

RUN go mod download

COPY src/ ./src

RUN CGO_ENABLED=0 GOOS=linux go build -a -installsuffix cgo -o ./bin/go-cart-api ./src

FROM scratch

COPY --from=builder /go-cart-api/src/database/*json /database/
COPY --from=builder /go-cart-api/bin/go-cart-api .

ENTRYPOINT  ["./go-cart-api"]
